---
- hosts: all
  sudo: yes
  tasks:
    - name: Add a new user
      user: name=t-hara

    - name: Install dependencies packages
      yum: name={{item}} state=latest
      with_items:
        - gcc
        - gcc-c++
        - java-1.8.0
        - curl-devel
        - expat-devel
        - gettext-devel
        - openssl-devel
        - zlib-devel
        - perl-ExtUtils-MakeMaker
        - libselinux-python

    - name: Check git source file existence
      command: ls /tmp/git-2.4.0.tar.gz
      ignore_errors: True
      register: git_source_exists

    - name: Download git source file
      get_url: url=https://www.kernel.org/pub/software/scm/git/git-2.4.0.tar.gz dest=/tmp/git-2.4.0.tar.gz
      when: git_source_exists|failed

    - name: Unarchive git.tar.gz
      command: chdir=/tmp/ tar zxf git-2.4.0.tar.gz
      when: git_source_exists|failed

    - name: Check git is installed
      command: which git
      ignore_errors: True
      register: git_is_installed

    - name: Configure git source
      command: chdir=/tmp/git-2.4.0 ./configure --prefix=/usr/local
      when: git_is_installed|failed

    - name: Make git
      command: chdir=/tmp/git-2.4.0 make prefix=/usr/local all
      when: git_is_installed

    - name: Install git
      command: chdir=/tmp/git-2.4.0 make prefix=/usr/local install

#================================================
# Install td-agent2
#================================================
- hosts:
    - fluentd
    - es
    - nk
  sudo: yes
  tasks:
    - name: Download td-agent2 installer
      get_url: url=https://td-toolbelt.herokuapp.com/sh/install-redhat-td-agent2.sh dest=/tmp/install-redhat-td-agent2.sh
    - name: Change mode of td-agent2 installer
      file: path=/tmp/install-redhat-td-agent2.sh mode="u=rwx,g=rw,o=rw"
    - name: Execute td-agent2 installer
      shell: /tmp/install-redhat-td-agent2.sh

#================================================
# Copy fluentd config files
#================================================
- hosts: fluentd
  sudo: yes
  tasks:
    - name: Copy config file
      copy: src=./files/fluentd-aggregator.conf dest=/etc/td-agent/td-agent.conf

- hosts: es
  sudo: yes
  tasks:
    - name: Copy config file
      copy: src=./files/fluentd-es.conf dest=/etc/td-agent/td-agent.conf

- hosts: nk
  sudo: yes
  tasks:
    - name: Copy config file
      copy: src=./files/fluentd-nk.conf dest=/etc/td-agent/td-agent.conf

#================================================
# Install Elasticsearch
# ref: http://qiita.com/dayflower/items/21c64a6f5da155a5ade3
#================================================
- hosts: es
  sudo: yes
  tasks:
    - name: Download rpm file and install Elasticsearch
      yum: name=https://download.elastic.co/elasticsearch/elasticsearch/elasticsearch-1.5.2.noarch.rpm state=present
    - name: Download kibana
      get_url: url=https://download.elastic.co/kibana/kibana/kibana-4.0.2-linux-x64.tar.gz dest=/tmp/kibana-4.0.2-linux-x64.tar.gz
    - name: Unarchive kibana.tar.gz
      command: tar zxf /tmp/kibana-4.0.2-linux-x64.tar.gz
    - name: Move kibana
      command: mv /home/vagrant/kibana-4.0.2-linux-x64 /usr/local/bin/

#================================================
# Install Norikra
#================================================
- hosts: nk
  user: vagrant
  tasks:
    Install rbenv
    - name: Clone rbenv.git
      git: repo=https://github.com/sstephenson/rbenv.git dest=~/.rbenv
    - name: Clone ruby-build
      git: repo=https://github.com/sstephenson/ruby-build.git dest=~/.rbenv/plugins/ruby-build
    - name: edit .bash_profile 1
      shell: echo 'export PATH=$HOME/.rbenv/bin:$PATH' >> ~/.bash_profile
    - name: edit .bash_profile 2
      shell: echo 'eval "$(rbenv init -)"' >> ~/.bash_profile
    - name: apply .bash_profile
      shell: source ~/.bash_profile

    Install ruby and jruby
    - name: install ruby
      command: $HOME/.rbenv/bin/rbenv install 2.2.2
    - name: install jruby
      command: $HOME/.rbenv/bin/rbenv install jruby-1.7.20

    Install norikra
    - name: make workspace for norikra
      file: path=~/norikra state=directory owner=vagrant group=vagrant mode=0755
    - name: copy Gemfile
      copy: src=./files/Gemfile dest=~/norikra/Gemfile
    - name: set local ruby version
      command: chdir=~/norikra $HOME/.rbenv/bin/rbenv local jruby-1.7.20
    - name: install bundler
      command: chdir=~/norikra $HOME/.rbenv/versions/jruby-1.7.20/bin/gem install bundler
    - name: install norikra
      command: chdir=~/norikra $HOME/.rbenv/versions/jruby-1.7.20/bin/bundle install

#================================================
# Install Gitbucket
#================================================
- hosts: git
  sudo: yes
  tasks:
    - name: Add User for tomcat
      user: name=tomcat shell=/sbin/nologin
    - name: Download tomcat
      get_url: url=http://ftp.yz.yamagata-u.ac.jp/pub/network/apache/tomcat/tomcat-8/v8.0.23/bin/apache-tomcat-8.0.23.tar.gz dest=/tmp/apache-tomcat-8.0.23.tar.gz
    - name: Unarchive tomcat
      shell: chdir=/tmp/ tar zxf apache-tomcat-8.0.23.tar.gz
    - name: Move tomcat
      shell: mv /tmp/apache-tomcat-8.0.23 /usr/local/tomcat

    - name: Download Gitbucket
      get_url: url=https://github.com/takezoe/gitbucket/releases/download/3.2/gitbucket.war dest=/usr/local/tomcat/webapps/gitbucket.war
    - name: Change owner tomcat directory
      file: path=/usr/local/tomcat owner=tomcat group=tomcat recurse=yes
